# Status

> Where Dharma is **right now**. The SHIPPER agent updates this file after every successful ship (mandatory per ADR-041, formerly ADR-026); Main Claude updates the "Feature in flight" line at handoffs.
>
> Read this first when starting a fresh session.

## Snapshot

- **Branch:** `main` at `b541530`
- **Last ship:** Timer brick type (2026-07-01): third measurement axis added to bricks — `{ kind: "timer"; targetMin; elapsedSec }` (reverses ADR-043's removal). Running/paused state is UI-local in `BrickChip` (`TimerControls` play/pause + mm:ss readout); only committed elapsed seconds persist via new `SET_TIMER_ELAPSED` action, so `brickPct()` stays pure and no wall-clock state goes stale on reload. Additive valibot variant — no schema version bump; v3 tick/units data still validates. AddBrickSheet gains a Timer chip + "Goal (minutes)" field. `onTimerCommit` threaded through Timeline/TimelineBlock/LooseBricksTray/TimedLooseBrickCard. Commit: `b541530`. **Also repaired pre-existing red on `main`** from earlier same-session UI passes (surfaced by the full 1726-test suite): the `Log Brick`→`Log` rename + repurpose (dock pill now toggles Log mode — a status banner — instead of opening AddBrickSheet), button/input height unification (`h-11`→`h-[48px]`), and masked `TimeInput` digit-value format; affected unit + e2e specs updated to the current contract. Prior: Log highlight flow (2026-07-01): tapping the dock `Log` pill enters Log mode — neon-green highlights every started-but-unlogged block/brick, with a "N to log" banner; commit `9904144`. Prior: edit-mode-on banner (2026-07-01): neon-green "EDIT MODE ON" status banner slides in below the header while edit mode is active; commit `59fcbdf`. Prior: Universal design pass (2026-07-01): `Input.tsx` h-[48px]+rounded-[8px]+card-edge border; `Chip.tsx` tracking-[0.08em]; `TopBar.tsx` rounded-[8px] icon buttons; `UnitsEntrySheet` + `SettingsSheet` confirm buttons → `<Button>` component; row buttons 10px→8px radius, 52px→48px height. All interactive elements now share 8px radius, 48px height, 0.08em tracking. Commit: `b1055e2`. Prior: Unified button design (2026-07-01): `Button.tsx` cva — 8px radius, 48px height, `tracking-[0.08em]`, secondary variant uses `card-edge`/`ink-dim` tokens; `AddChooserSheet` Cancel migrated from raw `<button>` to `<Button variant="ghost">`. Commit: `0f75d6f`. Prior: TimeInput on AddBrickSheet (2026-06-30): brick duration Start/End fields now use same --:-- masked TimeInput as AddBlockSheet. Masked time entry now universal across all time fields in the app. Prior: TimeInput masked field (2026-06-30): AddBlockSheet Start/End now show --:-- mask with fixed colon; only 4 digit slots editable. New component: components/ui/TimeInput.tsx. Prior: E2E sci-fi → real conversion COMPLETE (2026-06-29): ALL guard-skip tests across all milestones converted to real addInitScript-seeded assertions. Only intentional M10 SpeechRecognition feature-detection guards remain. Final commit: f739bd1 (m4f/m6/m7c/m7d/m9d/m9e).
- **Last preview URL:** `https://integrity-pink.vercel.app/` (main-branch production alias). Sandbox `curl -I` returns 403 `x-deny-reason: host_not_allowed` (Vercel Deployment Protection); loads normally in signed-in browser sessions.
- **Persisted schema: v3** (ADR-045 + M5). `AppState`/`PersistedState` now carry `deletions: Record<string,true>`. Additive lossless v2→v3 migrator. `loadState` migrates v1→v2→v3 with no data loss.
- **ADR-047 (M5):** `currentDayBlocks.ts` resolves `deletions` only; `appliesOn` Day-view wiring is deferred to a dedicated spec entry.
- **Proposed ADR-048 (M6 — not yet written):** "Post-dispatch readback for optimistic UI rejection uses an incrementing sequence + `useEffect` (not microtask) so React's batched re-render flushes before detection." Draft ADR to be authored in a follow-up session.
- **ADR-046:** period-aggregate helpers are pure — they derive "today" from `state.currentDate`, never the wall clock.
- **Methodology:** The Loop (SDD-outside, TDD-inside) per ADR-025; **single human gate** (preview tap-test only) per ADR-041 — the VERIFIER agent now replaces the planning gate; per-phase commit prefixes per ADR-027.

### R7 root-cause infrastructure (in force, ADRs 052/053/054)

- **Persist boundary**: `lib/persist.ts` now validates SHAPE via `lib/persistSchemas.ts` (valibot) on load. Per-field recovery preserves good fields; corrupt fields reset to defaults; UI shows a toast. `loadStateWithReport()` exposes the recovery log. `saveState()` validates the payload against the v3 schema in dev only.
- **TZ test infrastructure**: `npm run test:tz` runs the same `lib/**/*.tz.test.ts` glob under FOUR pinned timezones (PT, Tokyo, UTC, Nepal +5:45) in sequence. CI `tz-tests` matrix job runs them in parallel.
- **CI gate**: `.github/workflows/ci.yml` runs lint (with ratchet at 19 warnings) + typecheck + unit + TZ×4 + e2e + a11y + doc-refs. `ci-gate` is the aggregate required check.
- **Doc-ref integrity**: `npm run check:docs` validates every backtick-wrapped commit hash and `path/file.ext:NNN` reference across the docs tree. Ratchet locked at 271 broken refs (legacy m{N}-shard drift); CI fails if count grows.
- **Pre-commit TZ guard**: `.lintstagedrc.json` runs `scripts/check-tz-coverage.mjs` on every staged `lib/*.ts`. Date-touching files without a sibling `*.tz.test.ts` either pass with a baseline warning (existing) or fail hard (new).
- **Spec AC supersession convention**: each `m{N}/spec.md` carries a `### Supersessions` block listing ACs amended by later milestones + the ADR that records the drift. Applied to m1 today.
- **Hero pre-hydration skeleton**: `<Hero hydrated={false}>` shows em-dash placeholders for the date/dayNumber rows during the SSR-vs-CSR clock skew window (ADR-023). Layout stable, aria-busy true.
- **Mutation testing**: `npm run test:mutation` runs Stryker over `lib/dharma.ts` + `lib/dayOfYear.ts` + `lib/timeOffset.ts`. CI job `mutation` runs weekly + on PRs touching `lib/`; currently advisory (not blocking).

## Plan in force

- `phase1plan.md` (root, in `.prettierignore`) — **11-milestone build order M0..M10** with carried-forward decisions, locked AppState schema, and open spec gaps. Supersedes the previous 8-feature empty-toolkit pivot.
- The 94 existing test IDs from the previous pivot have been tagged `[survives]` / `[re-author]` / `[obsolete]` by the M0 PLANNER dispatch. Migration complete.

## Milestones

| Milestone                                              | State                                              | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ------------------------------------------------------ | -------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **M0 — Design System**                                 | **Shipped + tap-tested + hardened**                | 10 primitives + tokens. Gate #2 closed 2026-05-05. 5-round post-ship code-review loop (2026-05-23) found and fixed 47 issues across Button/Modal/Sheet/Stepper/BlockCard/layout/harness; convergence 24→12→5→1→0. +12 new test IDs (C-m0-024..035); 60 IDs closed total. 1582/1582 vitest. See `docs/milestones/m0/CHANGELOG.md`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| **M1 — Empty Building Shell**                          | **Shipped + tap-tested + hardened**                | Spatial 24-hr timeline, amber now-line, hero, top bar, Blueprint Bar, locked empty-state, floating dock. 56 IDs closed in original ship. Gate #2 closed. 6-round post-ship code-review loop (2026-05-23) found and fixed 33 issues across Timeline/BlueprintBar/TopBar/BottomBar/Hero/NowLine/DayCell + time-math libs; convergence 13→8→8→3→1→0. P0 caught in R2 (DST Jan-1 negative-UTC bug). New shared `isoToLocalDate` helper; ADR-049/050/051 added. 1589/1589 vitest, 20/20 TZ-pinned. See `docs/milestones/m1/CHANGELOG.md`.                                                                                                                                                                                                                                                                                                                        |
| **M2 — Add Block Flow**                                | **Shipped + tap-tested**                           | First interactive verb. Floating `+` + 24 slot-tap targets. AddBlockSheet, CategoryPicker, inline category creation, validation, Blueprint Bar non-empty path. 50 IDs closed. Gate #2 closed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| **M3 — Add Brick + Live Scoring**                      | **Shipped + tap-tested**                           | Add Brick flow, HeroRing, LooseBricksTray, BrickChip, live scoring, cross-up hook. 57 IDs closed. Gate #2 closed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| **M4a — Tick Brick Logging**                           | **Shipped to preview — awaiting Gate #2 tap-test** | First user-driven verb. Tap tick brick → done. Cascade: chip fill, glyph, scaffold, HeroRing, BlueprintBar. Triggers M3 celebrations (bloom/chime/fireworks). 41 IDs closed. Playwright+axe deferred to preview.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| **M4b — Goal Brick Stepper**                           | **Shipped to preview — awaiting Gate #2 tap-test** | Stepper verb for goal bricks. GoalStepperChip with −/+ buttons, 500ms long-press auto-repeat, light haptic per tick, medium haptic on clamp, scale-press feedback, reduced-motion respected. Reuses M4a celebrations for goal-driven path. 27 IDs closed. E2E converted to real addInitScript-seeded assertions (2026-06-29).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| **M4d — Add Chooser Sheet**                            | **Shipped to preview — awaiting Gate #2 tap-test** | Chooser sheet routing for dock `+` and slot tap. New `<AddChooserSheet>` with real focus trap, reduced-motion respected. Inside-block `+ Add brick` and tray `+ Brick` pill bypass chooser. M2/M3 migrations walk through chooser. 25 IDs closed. E2E converted to real addInitScript-seeded assertions (2026-06-29).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| **M4c — Time Brick Timer**                             | **Retired by M4f (ADR-043)**                       | Timer verb for time bricks (M4c) was shipped to preview; the time kind and all timer infrastructure were removed in M4f per ADR-043. M4c test IDs (25) explicitly retired in tests.md.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| **M4e — Brick Duration + Overlap**                     | **Shipped to preview — awaiting Gate #2 tap-test** | Universal `hasDuration` toggle on every brick kind. `lib/overlap.ts` half-open overlap engine. Overlap warning chip in AddBrickSheet + AddBlockSheet with Save-disable. `<TimedLooseBrickCard>`, `<BrickChip>` time-window badge. `selectTimelineItems` + `selectTrayBricks` selectors. ADR-042 locked. M2 block↔block enforcement closed retroactively. 731/731 vitest. E2E converted to real addInitScript-seeded assertions (2026-06-29).                                                                                                                                                                                                                                                                                                                                                                                                                |
| **M4f — Brick Model Rework**                           | **Shipped to preview — awaiting Gate #2 tap-test** | Collapsed bricks to two kinds (tick + units, free-text unit); removed M4c timer; manual-number entry via UnitsEntrySheet. `kind:"goal"`→`units`; `count`→`done`; `kind:"time"` removed; `runningTimerBrickId` removed; 4 timer actions + `LOG_GOAL_BRICK` removed; `SET_UNITS_DONE` added. 682/682 vitest across 56 files. 44 M4f IDs added; 25 M4c IDs retired. ADR-043. E2E converted to real addInitScript-seeded assertions (2026-06-29).                                                                                                                                                                                                                                                                                                                                                                                                               |
| **M4g — Dead-code Sweep**                              | **Shipped to preview — awaiting Gate #2 tap-test** | Removed 10 inert `@deprecated` props across 5 components; relabelled mislabelled test `U-m4f-016` → `U-m3-013`; removed stale `lib/timer.ts` reference in `lib/data.ts` comment. Net +3/−35 LOC. Zero behavior change. 682/682 vitest (unchanged). Pre-M5 cleanup complete.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| **M8 — Persistence**                                   | **Shipped to preview — awaiting Gate #2 tap-test** | `lib/persist.ts` (`loadState`/`saveState`/`PersistedState`/`dharma:v1` key); `usePersistedState()` hook; two-pass hydration in BuildingClient; `AppState.programStart` (ISO date, first-run stamp); `schemaVersion: 1`; top-bar day number now `programStart`-relative ("Building N"). ADR-044. 722/722 vitest across 58 files (+40/+2). Zero UI surface change. **RESOLVES: page refresh wipes state.** E2E converted to real addInitScript-seeded assertions (2026-06-29).                                                                                                                                                                                                                                                                                                                                                                                |
| **M9a — appliesOn resolver**                           | **Shipped to preview — awaiting Gate #2 tap-test** | Pure `appliesOn(recurrence, date): boolean` in `lib/appliesOn.ts`. Branches on all four `Recurrence` kinds. TZ-safe `parseLocalDate` helper. TZ-pinned test infra: `lib/appliesOn.tz.test.ts` + `vitest.tz.config.ts` + `npm run test:tz`. 760/760 vitest default suite (59 files); 11/11 TZ-pinned suite. First of five M9 chunks. M4e stored recurrence but never resolved it — M9a closes that gap.                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| **M9b — Day rollover + history store**                 | **Shipped to preview — awaiting Gate #2 tap-test** | `lib/history.ts` — pure `rollover(state, todayISO)`: archives finished day into `history` map, seeds fresh day (recurring bricks reset to incomplete; just-today bricks dropped; blocks carry forward only if still applicable; categories verbatim). `AppState` gains `currentDate` + `history: Record<ISO-date, ArchivedDay>`. `loadState` migrates v1 → v2 with no data loss. ADR-045. `usePersistedState` runs rollover on mount. 806/806 vitest across 60 files; TZ-pinned 11/11. Closes U-m9b-001..021, C-m9b-001..006, E-m9b-001..003. E2E converted to real addInitScript-seeded assertions (2026-06-29). **M9c is now unblocked.**                                                                                                                                                                                                                 |
| **M9c — Month view (Kingdom) + view switcher**         | **Shipped to preview — awaiting Gate #2 tap-test** | Apple/Google-Calendar-style month grid where every day cell shows that day's score (heat-fill intensity + numeral). Day/Week/Month/Year segmented view switcher — Day and Month live, Week and Year disabled until M9d/M9e. Prev/next month navigation. Tapping today's cell opens editable Building view; tapping a past day with history opens read-only `PastDayDetail`. New `app/(building)/AppShell.tsx` owns in-app `view` state and calls `usePersistedState` once; `BuildingClient` refactored to receive `state`/`dispatch` as props. New `lib/monthGrid.ts` (UTC-drift-free month date math) and `dayScore()` + `NO_DATA` in `lib/history.ts`. 913/913 vitest across 66 files; TZ-pinned 11/11. Closes U-m9c-001..012, C-m9c-001..016, A-m9c-001..004, E-m9c-001..003. E2E + a11y converted to real addInitScript-seeded assertions (2026-06-29). |
| **M9d — Week view (Castle) + period scoring**          | **Shipped to preview — awaiting Gate #2 tap-test** | Vertical seven-day (Sun→Sat) Castle view with per-day scores + week aggregate. `weekScore(state, anchorISO)` in `lib/history.ts` — first period aggregate; honest-scoreboard rule (missed in-range day = 0, future/pre-programStart days excluded). `lib/weekGrid.ts` (UTC-drift-free week date math). ViewSwitcher **Week** segment now live (Year stays disabled until M9e). Prev/next week nav; tapping a past day reuses M9c's `PastDayDetail`. ADR-046 (period-aggregate helpers are pure). 995/995 vitest across 69 files; TZ-pinned 11/11. Closes U-m9d-001..009, C-m9d-001..014, A-m9d-001..003, E-m9d-001..003. E2E + a11y converted to real addInitScript-seeded assertions (2026-06-29). **M9e now unblocked.**                                                                                                                                  |
| **M9e — Year view (Empire) — FINAL M9 CHUNK**          | **Shipped to preview — awaiting Gate #2 tap-test** | 3×4 twelve-month grid; each `MonthCell` shows that month's score. `YearAggregate` ring shows year score. `monthScore()` + `yearScore()` in `lib/history.ts` (ADR-046 pattern). `lib/yearGrid.ts` (UTC-drift-free year/month date math). ViewSwitcher **Year** segment now live — **all four segments (Day/Week/Month/Year) active, none disabled**. Tapping a month opens M9c Month view at that month (`initialMonth` prop + `AppShell` `monthTarget`). Amended C-m9c-012, C-m9d-010, C-m9d-011, A-m9d-002, A-m9c-002 for now-live Year segment. 1078/1078 vitest across 72 files; TZ-pinned 11/11. Closes U-m9e-001..009, C-m9e-001..012, A-m9e-001..002, E-m9e-001..003. E2E + a11y converted to real addInitScript-seeded assertions (2026-06-29). **MILESTONE 9 COMPLETE — the calendar is whole: Day/Week/Month/Year all live.**                      |
| **M9 — Calendar Nav**                                  | **COMPLETE** (all five chunks shipped)             | M9a (appliesOn) + M9b (rollover+history) + M9c (month/Kingdom) + M9d (week/Castle) + M9e (year/Empire). The full period-scoring ladder `dayScore → weekScore → monthScore → yearScore` is implemented. All four view-switcher segments live.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| **M5 — Edit Mode + Delete**                            | **Shipped to preview — awaiting Gate #2 tap-test** | Pencil-toggle Edit Mode (Locked/Unlocked). iOS-style jiggle + always-visible `×` on every block/brick. `<DeleteConfirmModal>` (recurring: Just today / All recurrences; non-recurring: single Delete). `lib/currentDayBlocks.ts` filters by `deletions`. Schema v2→v3 (`deletions` map). 1174/1174 Vitest across 75 files; `test:tz` 11/11. Closes U-m5-001..014, C-m5-001..020, A-m5-001..003, E-m5-001..004. E2E converted to real addInitScript-seeded assertions (2026-06-29).                                                                                                                                                                                                                                                                                                                                                                          |
| **M6 — Drag Reorder**                                  | **Shipped to preview — awaiting Gate #2 tap-test** | Visible drag handle on blocks + bricks in Edit Mode. Block drag-to-retime: 30-min snap, overlap rejection with medium-haptic snap-back + aria-live announce, reduced-motion path. In-block brick reorder via Framer `Reorder.Group`. New `lib/snapToSlot.ts` helper; `REORDER_BLOCK` + `REORDER_BRICK_IN_BLOCK` reducer arms. `Timeline.tsx` threads `modalOpen` to suppress drag while delete modal is open. No schema bump; `state.history` untouched (ADR-045). 1249/1249 Vitest across 79 files; `test:tz` 11/11. 22 warnings (all carryover). Closes U-m6-001..014, C-m6-001..011. E-m6-001..004 + A-m6-001..002 deferred-to-preview. **Proposed ADR-048** logged (see Open loops). Date shipped: 2026-05-20.                                                                                                                                          |
| **M7a — Stagger Fade-in + Skeleton Shimmer**           | **Shipped to preview — M7 chunk 1 of 5**           | Render-layer only. Per-surface stagger cascade on first hydrated paint of Day-view; `<Skeleton>` shimmer during two-pass-hydration window. New APIs: `lib/motion.ts` `staggerForCount`, `usePersistedState` 3-tuple, `components/Skeleton.tsx`, `stagger?` props on BlueprintBar/Timeline/LooseBricksTray, `hydrated?` on BuildingClient. Resolves SG-m7a-01/02/03. 1302/1302 Vitest across 83 files; `test:tz` 11/11. E-m7a-001..006 + A-m7a-001..003 deferred-to-preview. Date: 2026-05-20.                                                                                                                                                                                                                                                                                                                                                               |
| **M7b — Now-line Glow + Active-block Pulse + NOW Tag** | **Shipped to preview — M7 chunk 2 of 5**           | Render-layer only. `lib/activeBlock.ts` pure helper; `components/NowTag.tsx` amber "NOW" badge; `is-active` class with `@keyframes nowPulse` (1.8 s box-shadow halo); expanded NowLine drop-shadow halo; PRM collapse to static outline. `activeId` computed inside `<Timeline>`; no schema bump (still v3). Resolves SG-m7b-01/02/03. 1397/1397 Vitest across 85 files; `test:tz` 11/11. 19 lint warnings (net-zero vs M7a after cleanup). E-m7b-001..005 + A-m7b-001..003 deferred-to-preview. Date: 2026-05-20. Ship commit: `874ff35`.                                                                                                                                                                                                                                                                                                                  |
| **M7c — Hero % Count-up on First Load**                | **Shipped to preview — M7 chunk 3 of 5**           | Render-layer only. `motionTokens.countUp = { durationMs: 1600, easing: "easeOut" }` (8th token); `<HeroRing>` gains `firstPaintCountUp?: boolean` prop + joint-state `displayPct` driving stroke-dashoffset AND numeral via children-as-function extension (`children?: ReactNode \| ((displayPct: number) => ReactNode)`); imperative `animate(0, pct, { ease: [0, 0, 0.2, 1] })` from `motion/react`; PRM collapses to instant snap. Reuses M7a's `useFirstPaintAfterHydration` ref-machine. SG-m7c-01/02/03 resolved. Lint cleanup: 35 → 19 warnings (−16 via `chore(ship-m7c)` commit). 1435/1435 Vitest across 87 files. Ship commit: `a517fad`. Date: 2026-05-20.                                                                                                                                                                                     |
| **M7d — Block-100% Bloom + Day-100% Fireworks**        | **Shipped to preview — M7 chunk 4 of 5**           | `useBlockCelebrationOnce` (Set<string> mount-scoped) + `useDayCelebrationOnce` (boolean ref) + `celebrate(kind, { withAudio })` shim (audio deferred to M7f). `<DayCompleteCard>` (PRM fallback, role="status" aria-live="polite", 2000ms). `<TimelineBlock>` bloom + `.bloom-overlay-reduced` for PRM. `<BuildingClient>` day fireworks, extended PRM-conditional 1700→2000ms. `<Fireworks>` UNCHANGED (C-m7d-015 verified). `useCrossUpEffect` retained for compat. No schema bump. +44 tests (1435→1479); 19 warnings; 0 TS errors. Audio (M7f follow-up). C-m4f-009 sanctioned amendment. E-m7d-001..006 + A-m7d-001..003 deferred-to-preview. Date: 2026-05-22.                                                                                                                                                                                        |
| **M7e — Polish Layer (final chunk)**                   | **Shipped to preview — AWAITING GATE #2**          | FirstBrickCard (narrative payoff; 3 s auto-dismiss; `firstBrickShown` back-fill). BrandMarkLongPress + YearHeatmapPreview (600 ms long-press easter egg; z-40 overlay; PRM-safe). Toaster (module-level emitter; kind-discriminated ARIA; z-30 singleton in AppShell). Toast wiring on block-add/delete + brick-add/delete. +73 tests (1479→1552); 19 warnings; 0 TS errors. E-m7e-001..007 + A-m7e-001..005 converted to real addInitScript-seeded assertions (2026-06-29). **Gate #2: user taps the preview now.**                                                                                                                                                                                                                                                                                                                                        |
| **M7 — Polish Layer**                                  | **COMPLETE** (5/5 chunks shipped: M7a–M7e)         | M7a (stagger/skeleton) + M7b (now-line/pulse/tag) + M7c (hero count-up) + M7d (bloom/fireworks) + M7e (FirstBrickCard/BrandMarkLongPress/Toaster). Render-layer polish complete.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| **M10 — Voice Log**                                    | **Shipped to preview — awaiting Gate #2 tap-test** | Web Speech API (zero cost, no keys). `lib/speechRecognition.ts`, `lib/useVoiceCapture.ts`, `components/MicButton.tsx`, `components/VoiceCaptureOverlay.tsx`. `defaultTitle` prop on `AddBrickSheet`. Three failure-mode toasts (permission denied / no speech / unsupported). Hydration-safe via `useSyncExternalStore`. 56 IDs closed (U-m10-001..025, C-m10-001..018, E-m10-001..010, A-m10-001..003). 1722/1722 Vitest. E2E + a11y deferred-to-preview. **FINAL PHASE 1 MILESTONE.**                                                                                                                                                                                                                                                                                                                                                                     |

## Open loops

- **M7d open loops (carried forward):**
  - **PRM toggle mid-celebration leaves in-flight timer with original delay.** Documented limitation; acceptable. A future polish pass may cancel + restart the timer on PRM change.
  - **C-m7d-002 mutation-catch is indirect.** Deduplication proven via sister C-m7d-003. Non-blocking; behavior correct.
  - **Lint ceiling re-anchor at ≤ 20 is currently only in status.md prose.** Recommend ADR-049 to formally codify the ≤ 20 warning ceiling as a SHIP-blocker.

- **M6 open loops carried forward:**
  - **Keyboard / screen-reader drag alternative deferred** (SG-m6-06). No keyboard drag path exists; aria-live snap-back announcement is the sole a11y fallback for rejected moves. Documented as a11y debt; follow-up in a dedicated accessibility sprint.
  - **Per-day "just today" time override (SG-m6-02) considered + deferred.** Reordering a recurring block updates its template for all future occurrences. A `deletions`-style "just today" time-override map is out of scope for M6; follow-up if prioritized.
  - **Cross-container brick move (SG-m6-03) and loose-tray reorder (SG-m6-04) are out of scope.** Follow-up if prioritized by user.
  - **One inline `eslint-disable-next-line react-hooks/exhaustive-deps`** at `DraggableTimelineBlock.tsx:102` — `dropSeq` effect intentionally tracks only the sequence number. Flagged for future audit.
- **Proposed ADR-048 (not yet written):** "Post-dispatch readback for optimistic UI rejection uses an incrementing sequence + `useEffect` (not microtask) so React's batched re-render flushes before detection." Draft in next session.
- **Vercel MCP scope mismatch.** The MCP is loaded but authenticated to a Vercel account that is **not** a member of the `rahulranjith369-5644s-projects` team that owns `integrity`. Deploy verification must come from the GitHub commit-status or Vercel-bot PR comment. Never template the preview URL from the branch name.
- **Sandbox `next` module corruption** blocks `next dev` locally — preview env unaffected. Playwright + axe + ESLint + Lighthouse must be verified on the Vercel preview.
- **Mobile-Safari (WebKit) Playwright project disabled** per ADR-010. Re-enable when running in an environment with WebKit binaries present.
- **Hero `data-testid="building-counter"` missing (N3).** `C-bld-040` uses brittle class-name query; add testid in M7 polish.
- **DST off-by-one in `dayNumber()` (N4) — RESOLVED by M8.** `programStart`-relative day numbering with ISO-date math replaces calendar `dayOfYear()` for the Building counter. DST fixtures no longer needed for the day number feature.
- **Lint warning ceiling re-anchored to ≤ 20 warnings** (was spec AC #6 "≤ 13"). The project shipped M6 at 22 warnings and M7b at 19. M7c's `chore(ship-m7c)` cleanup dropped 16 warnings (35 → 19). The ceiling is re-set at **≤ 20 warnings** until a dedicated cleanup feature closes all carryover debt. Drift past 20 is a SHIP-blocker.
- **2 unsuppressed lint warnings:** `LooseBricksTray.tsx:43` and `TimelineBlock.tsx:62` render `aria-expanded` on `role="region"` / `role="article"` respectively. Spec-mandated by AC #44 + AC #45; intentional.
- **`harness.md` MCP rows still MISSING** for Vercel + Context7 + Playwright — wired in user's Claude account, not loaded into this session.
- **`lib/dharma.ts:duration()` returns 0 for no-end blocks** — intentional design; documented in CHANGELOG.
- **The Loop is now single-gate (ADR-041).** VERIFIER agent replaces Gate #1. Gate #2 (preview tap-test) is the only human gate.
- **`public/sounds/chime.mp3` is a 431-byte placeholder (SG-m4f-05) — superseded.** Web Audio API chimes (ADR-056) now provide real synthesis; the mp3 asset is unused. Safe to delete in a future housekeeping pass.
- **Eval debt (ADR-027 commit hygiene):** commit `f42d469` is subject-mislabeled "implement lib/weekGrid.ts" but actually batches `WeekView`/`WeekDayCell`/`AppShell` + their tests; `f871546` separately git-adds the orphaned-on-disk `lib/weekGrid.ts`; `88af719`/`33aa56e` are multi-ID batches. Per-test-group batching was plan-sanctioned (M9d plan § Commit strategy) — only the inaccurate `f42d469` subject is the defect. History-hygiene debt; no rebuild required.
- **Lint warnings 20 + 1 (M9e adds 1).** All benign: M9d's 4 (vestigial `archivedButtons` + 3 unused imports) carry forward; M9e adds 1 cosmetic warning — `lib/history.test.ts:2083` `makeM9eDay40` unused fixture helper. Optional `chore` cleanup; non-blocking.
- **Vacuous-pass debt — RESOLVED (2026-06-29):** All 45 guard-skip E2E tests across m4b/m4d/m4e/m4f/m5/m7e/m8/m9b/m9c/m9d/m9e converted to real addInitScript-seeded Playwright assertions. Playwright suite is now 282/282 real tests, 4 intentionally skipped, 0 vacuous-pass. a11y (axe-core) still deferred to Vercel preview (no Chromium in sandbox for `next dev` socket).
- **Deferred gap (ADR-047):** `appliesOn` Day-view wiring is deferred — the Day view still renders every block regardless of recurrence; `lib/currentDayBlocks.ts` resolves `deletions` only. A dedicated spec entry will close this gap.
- **M9e amended a fifth test ID beyond VERIFIER-sanctioned four.** `C-m9d-011` (`AppShell.test.tsx`) amended as legitimate mechanical inversion of a genuinely-stale "Year is inert" sub-test; EVALUATOR ruled it acceptable collateral of AC #8.
- **SG-m9b-03 — mid-session midnight crossing (documented deferral).** Rollover fires on load only, not while the app sits open across midnight. A future polish milestone may add an interval/visibility-change check. Non-blocking.
- **`var(--surface-2)` cleanup ticket (latent pre-M9c bug).** `components/HeroRing.tsx` and `components/TimelineBlock.tsx` reference `var(--surface-2)` which is NOT defined in `app/globals.css`. M9c/M9d/M9e correctly avoided this token. Tracked as a dedicated cleanup for a future `chore`.
- **Minor test-hardening backlog (non-blocking):** (1) `C-m9c-001` could add a `usePersistedState` spy to literally pin "invoked exactly once"; (2) wire `lib/monthGrid.test.ts` into `vitest.tz.config.ts` so `U-m9c-005` runs under a genuinely process-pinned TZ; (3) M9c test hardening backlog items carry forward.
- **Carry forward (still open):** M9a TZ-test pattern note; exhaustiveness-guard ADR; ADR-019 doc-drift; SG-m9b-03 mid-session-midnight deferral; `public/sounds/chime.mp3` placeholder (M7); `BrickBase` type module-private to `lib/types.ts`; C-m4e-030 shares green commit with C-m4e-027..029; JC2 inline it() blocks; 3 lint warnings in `lib/longPress.ts`; coverage debt C-m4e-029.
- **Trivial follow-up (non-blocking):** unused `_today` destructure in m9b spec RESOLVED (commit 286ece9). Remaining: 3 unused-import warnings in M9c test files; vestigial `archivedButtons` + 3 unused imports in M9d test files; `makeM9eDay40` unused fixture in `lib/history.test.ts:2083` (M9e).
- **`BrickBase` type module-private to `lib/types.ts`.** AddBrickSheet uses `as const` literals. Log if a third surface needs the shape.

**Resolved by M9e (closed loops):**

- **Year segment inert / Year view missing** — `YearView` (Empire) now live. All four view-switcher segments active. The `dayScore → weekScore → monthScore → yearScore` period-scoring ladder is complete. **RESOLVED.**

**Resolved by M8 (closed loops):**

- **Page refresh wipes the day / "state is lost on refresh"** — `localStorage` persistence under `dharma:v1` with `schemaVersion: 1` now survives tab close + reopen. **RESOLVED.**

**Resolved by M4g (closed loops):**

- **`@deprecated` dead props on `<TimelineBlock>` and peer components** — props deleted. M5 decision no longer needed.
- **JC1 — mislabelled test `U-m4f-016`** — relabelled `U-m3-013`. Cosmetic label hygiene resolved.
- **`lib/data.ts:45` stale `lib/timer.ts` comment** — reworded; stale reference removed.

**Resolved by M4f (closed loops):**

- `useTimer` exhaustive-deps lint warning — file deleted.
- `<TimerSheet>` auto-focus UX inconsistency — file deleted.
- `lib/blockValidation.ts:overlapsExistingBlock` dead production code — deleted in M4f.
- M4f architectural rework open loop — shipped.

## Quality gates (last full run — E2E real-conversion — 2026-06-29)

| Gate                       | Result                                                                                                                              |
| -------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| ESLint                     | 0 errors; 19 warnings (at ceiling of ≤ 20; all pre-existing carryover)                                                              |
| `tsc --noEmit`             | clean — 0 errors                                                                                                                    |
| Vitest (default)           | 1726/1726 passed                                                                                                                    |
| Vitest (TZ-pinned)         | 11/11 passed (`npm run test:tz`)                                                                                                    |
| Playwright (mobile-chrome) | 282/282 passed, 4 intentionally skipped, 0 failures. All guard-skip sci-fi tests converted to real addInitScript-seeded assertions. |
| Playwright (mobile-safari) | not run — WebKit unavailable (ADR-010)                                                                                              |
| axe-core                   | deferred to Vercel preview                                                                                                          |
| Lighthouse                 | not measured — verify on Vercel preview (target: Perf >90 / A11y 100)                                                               |

## Next intended action

**AWAITING GATE #2 — PHASE 1 COMPLETE.** E2E sci-fi → real conversion shipped (282/282 real Playwright tests). Vercel auto-deploys preview on push to main. User opens the preview at `https://integrity-pink.vercel.app/`, taps the live app, and reacts. That reaction feeds the Phase 2 roadmap or any follow-up feature invocation.
